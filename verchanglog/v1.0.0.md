# itdb v1.0.0 版本更新日志

**发布日期**: 2026-06-27  
**提交数量**: 25 次提交  
**版本类型**: 首个正式版本

---

## 📋 版本概述

v1.0.0 是 itdb 的首个正式版本，基于原 PHP + SQLite 项目重构为 Go + Vue3 前后端分离架构。该版本覆盖 IT 资产、软件许可、合同、发票、文件、机架、地点、字典、用户和系统设置等核心管理场景，并提供 Docker Compose 部署、自动备份、数据库迁移、LDAP 登录和操作审计能力。

---

## ✨ 新增特性

- 新增 IT 资产全生命周期管理，覆盖硬件、软件、合同、发票、文件、厂商和代理商
- 新增地点、区域、机架和机架可视化能力
- 新增本地账号与 LDAP 双模式登录
- 新增管理员完全访问和普通用户只读权限
- 新增操作历史、浏览历史和 Excel 导出
- 新增标签打印、二维码生成和多种标签纸预设
- 新增旧版 SQLite 数据库导入与自动迁移能力
- 新增每日 SQLite 自动备份和 Schema 变更前备份
- 新增 Docker Compose、Nginx、Supervisor 和容器入口部署配置
- 新增 README 项目截图、技术栈、项目结构、部署和 API 文档说明

---

## 🐛 Bug 修复

- 修复前端构建和类型检查相关问题
- 修复数据库迁移流程中的兼容性问题
- 修复资源与字典批量选择按钮缺失问题
- 修复资源列表统计信息位置不一致问题
- 更新相关图片和图标资源

---

## 🚀 性能优化

- 使用纯 Go SQLite 驱动，降低部署环境对 CGO 和系统库的依赖
- 通过 Vite 构建前端静态资源，提升开发和部署效率
- Docker 镜像采用多阶段构建，减少运行镜像体积

---

## 🔧 重构优化

- 后端重构为 Go 服务，统一 HTTP API、认证、数据访问和文件处理
- 前端重构为 Vue3 + TypeScript + Pinia + Vue Router 应用
- 整理 Docker、Nginx、Compose 和进程管理配置到 `deploy/` 目录
- 规范前端 Prettier 格式化配置和 TypeScript 工程配置
- 扩展 `.gitignore` 与 `.dockerignore`，排除运行时数据、构建产物和本地环境文件

---

## 📚 文档更新

- 完善 README 目录导航、项目介绍、技术栈、项目结构和功能清单
- 新增本地开发、Docker Compose 快速部署和生产环境部署说明
- 新增 API 文档、数据库说明、常见问题、安全建议、许可证、致谢和联系方式
- 新增 `docs/sqlite.txt` 数据库相关说明
- 新增 v1.0.0 版本日志

---

## 🔄 API 变更

- 首次提供 `/api` 前缀的后端接口
- 新增认证、资源管理、字典管理、报表、标签、备份、数据库导入、系统设置和健康检查接口
- 除登录接口外，业务接口默认通过 `Authorization: Bearer <token>` 访问

---

## 📊 数据库变更

- 首次提供 SQLite 数据库 Schema，共覆盖 36 张核心业务表
- 新增用户、设置、操作历史、浏览历史、资产、软件、合同、发票、文件、标签、地点、区域和机架等数据表
- 新增旧版 PHP ITDB 数据库导入与 Schema 迁移逻辑
- 新增自动备份目录与运行时数据目录约定

---

## 🔄 升级说明

这是首个正式版本，无历史版本升级要求。

部署前请注意：

1. 生产环境务必修改默认管理员密码 `admin / admin123`
2. 生产环境务必设置 `ITDB_JWT_SECRET`
3. 使用 Docker Compose 部署时，请参考 `deploy/.env.example` 创建 `deploy/.env`
4. 从旧版 PHP ITDB 迁移时，可通过系统内置数据库导入功能上传旧版 `.db` 文件
5. 请定期备份 `backend/data/` 或 Docker 持久化数据目录

---

## 👥 贡献者

- Jerion (416685476@qq.com)

---

## 📄 许可证

MIT License

---

**完整提交记录**: 25 次提交  
**开发周期**: 2026-03-13 至 2026-06-27 (107 天)
